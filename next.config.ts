// import type { NextConfig } from "next";

const nextConfig = {
  // output: 'export',
  trailingSlash: true,
  // experimental: {
  //   missingSuspenseWithCSRBailout: false,
  // },
};

export default nextConfig;
